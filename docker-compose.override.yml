##
# NOTE: This is typically a file generated by the local developer to define their own
# specific docker needs. This file is provided just as a courtesy to make Nurax a bit
# more turn-key.
##

version: '3'

services:
  # Map the application port to localhost
  server:
    ports:
      - 4000:4000
    expose:
      - 4000

  # Map the application port to localhost
  test:
    ports:
      - 4001:4001