<%-
  @page_title = "Contact DCA"
-%>

<div class="row-fluid">
  <div class="col-md-12"></div>

  <div class="row-fluid">

    <!-- nested LEFT -->
    <div class="col-md-3">
      <%= render :partial => 'pages/menu' %>
    </div>

    <!-- nested RIGHT -->
    <div class="col-md-6">
      <h2><%= @page_title %></h2>
      <p>The Tufts Digital Library welcomes and encourages your
        questions, comments, and feedback. We also provide <a href="http://sites.tufts.edu/dca/research-help/">research
        help including reference, requests for image reproduction, and access to the non-digital collections</a>.</p>

      <p>Your input is important to us and contributes to the development of the TDL. Please contact us
          in any of the following ways.</p>

      <h3>By feedback form:</h3>
      <% unless @errors.empty? %>
          <strong>Please fix the following form errors:</strong>
          <ul class="errors">
            <% @errors.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
      <% end %>

      <%= form_tag do %>
          <fieldset>
            <%= label_tag 'Your Name' %> <%= text_field_tag :name, params[:name] %>
            <%= label_tag 'Your Email' %> <%= text_field_tag :email, params[:email] %>
            <%= label_tag 'Your Message' %> <%= text_area_tag :message, params[:message] %>
            <%#= show_simple_captcha(:image_style => 'random', :label => "Enter the characters you see into the text field") %>
            <%= label_tag '' %><%= submit_tag 'Send!' %>
          </fieldset>
      <% end %>

      <h3>By mail, phone, or email:</h3>
      <p>Tufts Digital Library<br />
        c/o Digital Collections and Archives<br />
        Tisch Library Building<br />
        35 Professors Row<br />
        Medford, MA 02155<br />
        dl_feedback&#x0040;elist.tufts.edu<br />
        Phone: 617.627.3737</p>

      <div id="static_thumbnail">
        <a href="/concern/eads/m326mb85b"><%= image_tag('featured_collections/boy-writing.jpg', alt:'Boy Writing, Eliot-Pearson Department of Child Development', width: '250px') %></a>
      </div>
    </div>

    <div id="breadcrumbdivider">&nbsp;</div>
  </div>
</div>
